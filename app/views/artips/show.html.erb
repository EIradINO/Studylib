<div class="white_container">
  <div>
    <strong>Content:</strong>
    <%= @artip.content %>
  </div>

  <%= render 'tiplikes/tiplike' %>

  <%= link_to 'Edit', edit_artip_path(@artip) %> |
  <%= link_to 'Back', artips_path %>
</div>